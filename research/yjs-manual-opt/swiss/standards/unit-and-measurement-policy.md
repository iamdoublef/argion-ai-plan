# Swiss 单位与计量规范

## 总原则

- 以产品真实铭牌和技术资料为准。
- 说明书默认使用公制，必要时并列英制。
- 同一字段在同一产品、同一语言中必须保持一致。

## 规则

- 尺寸：优先 `mm`
- 重量：优先 `kg`
- 容量：优先 `L`
- 温度：使用 `°C`
- 噪音：使用 `dB(A)`
- 需要并列英制时，顺序为“公制 / 英制”

## 文本要求

- 数值和单位之间保留半角空格，例如 `120 mm`
- 范围使用 `~` 或 `–`，同一文档必须统一
- 不要把单位翻译成口语，例如不要写“公分”，统一写 `mm`
